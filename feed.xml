---
layout: null
permalink: /feed.xml
---
<?xml version="1.0" encoding="utf-8"?>
{% assign published_articles = site.articles | where_exp: "item", "item.draft != true" | sort: "date" | reverse %}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <link>{{ '/' | absolute_url }}</link>
    <atom:link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/rss+xml" />
    <lastBuildDate>{{ published_articles.first.date | default: site.time | date_to_rfc822 }}</lastBuildDate>
    <generator>Jekyll</generator>
    {% for post in published_articles limit: 50 %}
    <item>
      <title>{{ post.title | xml_escape }}</title>
      <description>{{ post.summary | default: post.excerpt | strip_html | strip_newlines | normalize_whitespace | xml_escape }}</description>
      <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
      <link>{{ post.url | absolute_url }}</link>
      <guid isPermaLink="true">{{ post.url | absolute_url }}</guid>
      {% if post.tags %}
        {% for tag in post.tags %}
      <category>{{ tag | xml_escape }}</category>
        {% endfor %}
      {% endif %}
    </item>
    {% endfor %}
  </channel>
</rss>
